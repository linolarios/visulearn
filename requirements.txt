# ---- Core contract ----
pydantic>=2,<3

# ---- Research (AGENT.md §1) ----
ddgs>=9                 # NOT duckduckgo-search (frozen Jul 2025, renamed to ddgs)
wikipedia-api>=0.6      # primary grounding source: deterministic, no cloud-IP blocking

# ---- LLM providers ----
# src/script_engine.py talks to Ollama / Gemini / Groq over their plain HTTP APIs
# behind one Provider interface, so no vendor SDK is required at runtime.
requests>=2.31          # the only transport dependency of the Script Engine

# ---- Assets ----
Pillow>=10
Pygments>=2.17
graphviz>=0.20          # Python bindings; ALSO needs the system `graphviz` package
manim>=0.18             # Community Edition; needs system LaTeX + ffmpeg

# ---- TTS ----
kokoro-onnx>=0.3        # verify exact package for your build; 24 kHz output
soundfile>=0.12

# ---- Assembly: PINNED to 1.x API used across the codebase (AGENT.md §3) ----
moviepy<2

# ---- Dev ----
pytest>=8
ollama>=0.3             # dev only: scripts/smoke_ollama.py. NOT used by src/script_engine.py.

# System dependencies (NOT pip — install separately):
#   ffmpeg, graphviz, LaTeX (for Manim), and Node.js + @mermaid-js/mermaid-cli
